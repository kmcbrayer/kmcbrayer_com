1. install pip
2. install django
3. install sqlite3

server:
install pip
pip install django==1.7
apt install python-dev
apt install sqlite3
pip installuwsgi
apt install nginx
	make nginx conf file
apt install git
make .bash_profile
	add dir to pythonpath. 

to run server 
	stop nginx
	uwsgi http :80 --wsgi-file /path/to/wsgi.py 

to update repo
	git pull origin develop
